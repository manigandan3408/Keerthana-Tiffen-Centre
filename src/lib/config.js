/**
 * Keerthana Tiffin Centre's own Google review page.
 *
 * This one address is used everywhere: the "Review us on Google" button, the
 * QR code on the landing page, and the QR code on the printable card (/qr).
 * It is also the address to program into the NFC card (see README).
 * There are no environment variables to set.
 */
export const GOOGLE_REVIEW_URL = 'https://g.page/r/CZVRWFme0k4tEAI/review';
